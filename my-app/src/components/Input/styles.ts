import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    height: 56,
    width: "100%",

    borderRadius: 5,

    backgroundColor: "#1F1E25",
    color: "#FFFF",

    padding: 16,
    marginBottom: 5,

    fontSize: 16,
  },
  error: {
    color: "red"
  }
});
